# Copyright 2018-2021 Crown Copyright
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

# Not interpreted by any java code, simply used by reference in the population.resources section
default:
  rootId: /data/local-data-store
  connectionDetail: data-service
  type: uk.gov.gchq.syntheticdatagenerator.types.Employee
  format: avro

population:
  userProvider: example
  users:
  - userId: Alice
    auths:
      public
      private
    roles:
      HR
    trainingCourses:
      PAYROLL_TRAINING_COURSE

  policyProvider: std
  policies:
  - resourceId: ${default.rootId}/with-policy/
    owner: Alice
    recordRules:
      "1-Bank details only visible for the purpose of salary": uk.gov.gchq.palisade.example.library.rule.BankDetailsRule
      "2-Emergency numbers only available for duty of care purpose": uk.gov.gchq.palisade.example.library.rule.DutyOfCareRule
      "3-Nationality is only available for staff report": uk.gov.gchq.palisade.example.library.rule.NationalityRule
      "4-Address masked for estates staff and otherwise only available for duty of care": uk.gov.gchq.palisade.example.library.rule.ZipCodeMaskingRule
      "5-Record content masked for all who are not in the employee's management chain or part of the estates or HR.": uk.gov.gchq.palisade.example.library.rule.RecordMaskingRule
    resourceRules:
      "1-Only HR can access the first resource": uk.gov.gchq.palisade.example.library.rule.FirstResourceRule
  - resourceId: ${default.rootId}/no-policy/
    owner: Alice
    recordRules:
      "1-Pass through rule for no-policy directory": uk.gov.gchq.palisade.example.library.rule.EmployeePassThroughRule
    resourceRules:
      "1-Pass through rule for no-policy directory": uk.gov.gchq.palisade.example.library.rule.PassThroughRule

  resourceProvider: std
  resources:
  - resourceId: ${default.rootId}/no-policy/employee_large.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/employee_small.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/employee_large.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/employee_small.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
    # if you want more resources, macros are your friend here
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000000.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000001.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000002.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000003.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000004.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000005.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000006.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000007.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000008.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000009.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000010.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000011.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000012.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000013.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000014.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000015.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000016.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000017.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000018.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000019.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000020.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000021.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000022.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000023.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000024.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000025.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000026.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000027.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000028.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000029.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000030.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000031.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000032.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000033.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000034.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000035.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000036.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000037.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000038.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000039.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000040.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000041.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000042.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000043.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000044.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000045.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000046.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000047.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000048.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000049.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000050.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000051.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000052.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000053.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000054.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000055.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000056.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000057.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000058.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000059.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000060.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000061.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000062.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000063.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000064.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000065.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000066.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000067.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000068.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000069.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000070.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000071.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000072.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000073.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000074.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000075.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000076.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000077.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000078.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000079.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000080.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000081.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000082.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000083.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000084.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000085.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000086.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000087.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000088.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000089.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000090.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000091.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000092.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000093.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000094.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000095.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000096.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000097.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000098.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/no-policy/many-employees/duplicate_000000/employee_000099.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000000.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000001.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000002.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000003.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000004.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000005.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000006.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000007.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000008.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000009.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000010.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000011.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000012.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000013.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000014.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000015.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000016.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000017.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000018.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000019.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000020.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000021.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000022.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000023.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000024.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000025.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000026.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000027.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000028.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000029.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000030.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000031.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000032.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000033.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000034.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000035.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000036.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000037.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000038.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000039.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000040.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000041.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000042.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000043.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000044.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000045.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000046.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000047.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000048.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000049.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000050.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000051.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000052.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000053.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000054.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000055.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000056.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000057.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000058.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000059.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000060.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000061.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000062.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000063.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000064.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000065.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000066.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000067.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000068.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000069.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000070.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000071.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000072.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000073.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000074.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000075.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000076.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000077.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000078.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000079.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000080.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000081.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000082.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000083.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000084.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000085.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000086.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000087.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000088.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000089.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000090.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000091.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000092.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000093.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000094.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000095.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000096.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000097.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000098.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}
  - resourceId: ${default.rootId}/with-policy/many-employees/duplicate_000000/employee_000099.avro
    rootId: ${default.rootId}/
    connectionDetail: ${default.connectionDetail}
    attributes:
      type: ${default.type}
      serialisedFormat: ${default.format}

  serialiserProvider: std
  serialisers:
  - flavourFormat: ${default.format}
    flavourType: ${default.type}
    serialiserClass: uk.gov.gchq.palisade.example.library.serialise.AvroSerialiser
